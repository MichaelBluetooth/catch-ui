import { ComentImagePipe } from './coment-image.pipe';

describe('ComentImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ComentImagePipe();
    expect(pipe).toBeTruthy();
  });
});
